# Copy this to .env for local development
# No quotes around values — just key=value
# NEVER commit .env to git

SECRET_KEY=replace-with-a-long-random-string
ADMIN_ID=your_admin_id
ADMIN_PASS=your_strong_password
DB_PATH=hostel.db
